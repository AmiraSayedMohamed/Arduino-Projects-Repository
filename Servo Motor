//this a code to control a servo motor

#include <Servo.h>

Servo myservo;
int pos = 0;

void setup()
  
{
  myservo.attach(10); 
}

void loop()
{
  for(pos = 0; pos <= 90; pos +=30)
    myservo.write(pos);  
}
 
